#!/bin/sh

current_date=$(date +%Y-%m-%d)
manifest_path=$(dirname "$1")
current_semver=$(grep -o "['"'"][0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+["'"']" "${manifest_path}/__manifest__.py" | head -n 1 | tr -d "'"'"')

cat << EOF
## $current_semver - $current_date

### 
EOF

